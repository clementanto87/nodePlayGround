pipeline {
  agent any
  
  stages {
    stage("build") {
      steps {
        echo 'building application'
      }
    }
    
    stage("deploy") {
      steps {
        echo 'deploy application'
      }
    }
    
    stage("run") {
      steps {
        echo 'run application'
      }
    }
  }
}
